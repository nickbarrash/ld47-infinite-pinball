using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeleportController : MonoBehaviour
{
    //GameObject gateFrom;
    //GameObject gateTo;

    //void Awake()
    //{
    //    gateFrom = transform.Find("GateFrom").gameObject;
    //    gateTo = transform.Find("GateTo").gameObject;
    //}

    //public void teleport(TeleportGate gate, GameObject pinball) {
    //    pinball.transform.position = pinball.transform.position + (getOtherGate(gate).transform.position - gate.transform.position);
    //}

    //private TeleportGate getOtherGate(TeleportGate gate) {
    //    return gate1 == gate ? gate2 : gate1;
    //}
}
